# Bounded, not bare `>=`. Every line was previously open-ended, so the host
# resolved a different set from the development machine on every build - and a
# major release could land in production without anyone choosing it.
#
# Floors exclude known-vulnerable releases (Werkzeug < 3.0 carries published
# CVEs). Ceilings stop a major version arriving unannounced. Exact pins with a
# lockfile are still the right end state; these bounds are what makes today's
# deploy reproducible enough to trust.

Flask>=3.0,<4
Werkzeug>=3.0,<4
Jinja2>=3.1.4,<4
Flask-SQLAlchemy>=3.1,<4
SQLAlchemy>=2.0.30,<3
Flask-Migrate>=4.0,<5
alembic>=1.13,<2
psycopg2-binary>=2.9.9,<3
Flask-WTF>=1.2,<2
WTForms>=3.1,<4
email-validator>=2.1,<3
Flask-Login>=0.6.3,<0.7
Flask-Bootstrap>=3.3,<4
gunicorn>=22.0,<24

# Reports and exports
openpyxl>=3.1,<4
reportlab>=4.1,<5
pandas>=2.2,<3
xlsxwriter>=3.2,<4

# Generating the PWA icon set
Pillow>=10.3,<12
